RTS COMMAND - LISTA DE PRUEBAS  #3
Generada 2026-08-16   ·   addon 0.10.0 · mod-rts 0.5.0 · rts_core 0.10.0

QUE HAY EN ESTA RONDA
  La sesion de la camara RTS y del raton. Todo esto esta compilado e
  instalado, pero NADA de ello se ha visto funcionar todavia.
  Tambien: FOV, borrado de las barras de vida, y el log que crecia sin parar.

COMO USARLA
  [x] funciona   ·   [!] falla   ·   [?] no lo entiendo / no estoy seguro
  Escribe al lado lo que has visto, aunque sea corto.
  Lo importante: si algo falla, lo que pongas en "notas" me dice DONDE
  esta el fallo. Cada apartado explica que significaria cada resultado.

ANTES DE EMPEZAR
  1. Enciende el worldserver     (ya tiene el binario nuevo puesto)
  2. Cliente nuevo + injector.exe   (la DLL SI ha cambiado esta ronda)
  3. /reload
  4. /console scriptErrors 1
     El WoW esconde los errores de Lua. Por eso un texto roto no imprimio
     nada en toda una sesion y parecia un fallo de logica.

EL ORDEN IMPORTA
  Haz la seccion A primero. Toca todas las ordenes, asi que si falla ahi,
  todo lo demas se lee mal.

HERRAMIENTAS
  /rts version   las tres versiones
  /rts debug     cada mensaje entre addon y servidor
  /rts units     que unidades esta publicando la DLL
  /rts cam show  los valores de encuadre de la camara


==============================================================================
A · EL RATON YA NO SE SECUESTRA   (el cambio mas grande)
==============================================================================
    El marco que se quedaba con el raton durante todo el modo RTS ahora
    solo se enciende mientras arrastras una caja. Por eso antes no se
    iluminaba nada al pasar el raton por encima.

    Lo unico que NO he podido comprobar yo: que el WorldFrame nos deje
    escuchar los clicks. Si no nos deja, no funcionara ningun gesto.

[X] A1  PASAR EL RATON POR ENCIMA DE UN ENEMIGO LO ILUMINA
    Entra en modo RTS y pasa el raton sobre un bicho. NO hagas click.
    Debe iluminarse, igual que fuera del modo RTS.
    >> Esta pregunta sola responde a toda la reforma. Si se ilumina, el
       raton esta libre y cualquier fallo posterior es otra cosa.
    notas: 

[X] A2  LOS GESTOS SIGUEN FUNCIONANDO
    - Click izquierdo en un bot        -> lo selecciona - Si
    - Shift+click en otro              -> lo anade - si
    - Arrastrar caja sobre varios      -> los selecciona - No, mueve la cámara.
    - Click derecho en el suelo        -> orden de mover -si
    - Arrastrar con el derecho         -> gira la cámara - si
    >> Si A1 funciono pero esto NO: es que el WorldFrame no nos deja
       escuchar los clicks. Dimelo tal cual, tengo un plan B y no es
       un cambio grande.
    notas: Click mas arrastrar no me deja hacer la caja de selección.

[!] A3  CLICK IZQUIERDO EN ALGO QUE NO ES TUYO LO PONE DE OBJETIVO
    Click izquierdo en un enemigo o un PNJ (no en tus bots).
    Debe quedar como tu objetivo, y tu seleccion NO debe cambiar.
    Antes: se borraba la seleccion. Solo el suelo debe borrarla.
    notas: Si pero salta un error "RTS command, se ha bloqueado de una accion solo disponible para la interfaz de usuario de Blizzard.


[!] A4  INTERACTUAR ACERCA AL BOT HASTA EL PNJ
    Selecciona un bot, click derecho en un PNJ.
    Debe ANDAR hasta quedarse a un metro y entonces hablar.
    Esto es lo que pediste en PRUEBAS-2 (A2): "deberia ser un move y
    luego interactuar". Ahora lo calcula el servidor, que es el unico
    que sabe las dos posiciones exactas.
    >> Si habla desde lejos: el worldserver no es el binario nuevo.
    >> Si anda pero no habla: se ha pasado de largo. Dime a que
       distancia se quedo.
    notas: Se queda lejos del pnj aun. si lo hacerco manualmente entonces si habla luego. 


==============================================================================
B · CAMPO DE VISION (FOV)   -- nuevo, sin estrenar
==============================================================================
    Aplana la perspectiva. El WoW usa 90 grados; el modo RTS ahora 60.
    Lo escribe la DLL, no el addon.

[X] B1  EL FOV CAMBIA
    /rts cam fov 40     debe verse muy plano, tipo isometrico
    /rts cam fov 90     normal
    /rts cam fov 0      quita el ajuste
    >> Si no cambia nada: o la DLL no esta inyectada (el comando avisa),
       o el cliente lo reescribe mas rapido que nosotros. Mira el log.
    notas: BRUTAL!

[!] B2  LA CAJA DE SELECCION SIGUE ACERTANDO CON EL FOV CAMBIADO
    >> Este es el riesgo de verdad de esta seccion.
    Con /rts cam fov 40, arrastra una caja alrededor de tus bots.
    Debe seleccionar exactamente los que has encerrado.
    La caja proyecta el mundo a la pantalla usando escalas sacadas del
    FOV. En teoria se ajusta solo; en la practica, si algo esta mal,
    aparece aqui primero.
    >> Si falla: dime el FOV y mas o menos cuanto se desviaba.
    notas: No funciona (en un previo check lo he comentado)


==============================================================================
C · DISTANCIA DE ATAQUE   -- cambiado hace dias, nunca comprobado
==============================================================================
    Visibilidad del servidor 100 -> 300, vista de los bots 100 -> 300,
    reaccion 150 -> 450, y la DLL publica 32 unidades a 180 metros en vez
    de 16 a 60. Lo reportaste roto ANTES del primer reinicio del server.

[!] C1  PUEDES MANDAR ATACAR A UN ENEMIGO LEJANO
    Busca un hostil a 80-150 metros y hazle click derecho.
    >> Normal: acepta la orden y van.
    >> Esperado: si esta a mas de ~150 no van. Es el limite propio de
       playerbots, no un fallo.
    >> Fallo: el click derecho no hace absolutamente nada. Mira
       /rts units para ver si la DLL lo esta publicando.
    notas:Ahora funciona perfecto pero creo que es porque hemos quitado lo que secuestrava el ratón. podemos quitar este ajuste de distancia de ataque. y ver si funciona igual? sino lo revertimos. 

[!] C2  EL MUNDO SIGUE FLUIDO EN ZONA CON GENTE
    Triplicar la visibilidad son muchas mas actualizaciones por cliente.
    >> Si notas tirones cerca de multitudes: baja
       Visibility.Distance.Continents a 150-200. Es lo primero a
       culpar de cualquier tiron nuevo.
    notas: Vamos a reducir esto a los valores de fabrica


==============================================================================
D · COSAS QUE NO DEBERIAN HABERSE ROTO
==============================================================================

[x] D1  EL ADDON CARGA LIMPIO
    Borre Plates.lua y edite Core.lua con un script. Pasa el chequeo de
    sintaxis, pero eso no es lo mismo que cargar.
    Entra al juego: sin errores de Lua, /rts responde, /rts help imprime.
    >> Si sale un error con "Plates": quedo una referencia.
    notas:

[x] D2  LAS PLACAS DE NOMBRE DEL WOW VUELVEN A SER NORMALES
    V y Shift+V deben encender/apagar nombres de enemigos y aliados como
    siempre. El codigo que forzaba esos ajustes ya no existe.
    notas:

[x] D3  FUERA DEL MODO RTS, LA CAMARA ES LA DE SIEMPRE
    Esta es la regla que pusiste tu: el modo RTS solo afecta al modo RTS.
    Al salir: tercera persona, zoom con la rueda con su alcance normal,
    ajuste automatico encendido, FOV normal, y tus teclas Q/E/X tuyas.
    >> Si algo se queda pegado, dime cual. Cada uno es un ajuste
       concreto y se restauran todos desde una sola lista.
    notas:

[x] D4  EL LOG YA NO CRECE
    Tras 10-15 minutos, mira el tamano de
    C:\Server\rts-client-mod\build\bin\rts_core.log
    Debe pesar unos pocos KB, no megas. Llego a 12,8 MB.
    notas:


==============================================================================
E · YA COMPROBADO -- no hace falta repetirlo
==============================================================================
  Los circulos en el suelo bajo los bots seleccionados.
  La camara RTS: desplazamiento plano, inclinacion que se queda,
  misma velocidad en todas las direcciones, Q/E suben y bajan, rueda con
  mucho recorrido, y el encuadre guardado como valor por defecto.
  La bengala con size 2.


==============================================================================
F · CALLEJONES SIN SALIDA -- no volver sin pruebas nuevas
==============================================================================
  COLOR POR UNIDAD EN LOS CIRCULOS. Dos compilaciones, dos negativos
  confirmados en juego. El segundo intento quitaba la variable del
  momento de lectura, asi que esa direccion sencillamente no es el color.
  Antes de tocarlo otra vez, lee el aviso en
  rts-client-mod/src/Offsets.h. Esta en pausa por decision tuya.


==============================================================================
SI SOLO TIENES PACIENCIA PARA TRES
==============================================================================
  1. A1  pasar el raton por encima ilumina   (responde a toda la reforma)
  2. A2  los gestos siguen funcionando       (lo unico que no pude probar)
  3. B2  la caja acierta con el FOV cambiado (el riesgo escondido)
