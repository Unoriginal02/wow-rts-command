PRUEBAS-24 -- EL CRASH, EL CASTEO QUE SE CORTABA Y LAS MEDIDAS
==============================================================

Estado al cerrar la sesion del 2026-09-05.
Addon 0.79.0, mod-rts 0.37.0, rts_core 0.10.0 (sin tocar).
TODO COMPILADO Y DESPLEGADO. **HAY QUE REINICIAR EL SERVIDOR.**

Esta ronda es la respuesta a `PRUEBAS-23`. El detalle tecnico esta en
`C:\Server\CLAUDE.md`, seccion del 2026-09-05.


LO QUE SE ARREGLA, EN UNA LINEA CADA COSA
-----------------------------------------

  1. EL CRASH. `CastAs` escribia en un nodo de mapa que acababa de liberar,
     cada vez que lanzabas por un bot distinto del anterior.
  2. El casteo que arrancaba y se cortaba a medio segundo (C6).
  3. Neferite (y tu heroe en general) sin hechizos (C2).
  4. El catalogo pasa a ser barra + todo lo que el personaje sepa (C2).
  5. Iconos mas pequenos, marcos y columna mas grandes, mas separacion.
  6. Los macros pierden el icono: solo texto (seccion E).
  7. El click derecho en una fila del grupo ya no hace primario (A5).
  8. LA SALA, rehecha contra el boceto retocado: columna estrecha, todo pegado
     a la izquierda, la fila de huecos llena el ancho, macros con borde y una
     raya vertical entre cada dos seleccionados.
  9. Los tres retoques de la segunda pasada: aire a la derecha, marcos un poco
     mas abajo, y la columna del estado B bajada y separada por dentro.
 10. **`UnitClass("player")` miente despues de un cambio de personaje**, igual
     que `UnitName`. El mago salia con el color del guerrero.

LO QUE SIGUE SIN VERSE: la cola (seccion D de la 23, que quedo `[-]` entera).
Se repite aqui, porque el crash pudo estar tapandola.


LO PRIMERO DE TODO
------------------

[ ] 0. Reiniciar el servidor y entrar. `/rts version` tiene que decir
       mod-rts 0.37.0 y addon 0.79.0.
       Si mod-rts sale 0.36.0 el worldserver viejo sigue corriendo y **el
       crash sigue puesto**: no sigas con la seccion A.
       notas:

[ ] 0b. Entrar en modo RTS. La sala llena, como la dejaste.
        notas:


A. EL CRASH -- LA PRUEBA IMPORTANTE
-----------------------------------

La causa era escribir en memoria liberada, y eso NO se cae en el acto: machaca
el monton y el servidor muere despues, en cualquier sitio. O sea que "no se ha
caido en dos minutos" no prueba nada. Hay que insistir.

[ ] A1. **Lanzar hechizos ALTERNANDO DE PERSONAJE, muchas veces seguidas.**
        Selecciona al mago, pulsa un hueco. Selecciona a la sanadora, pulsa un
        hueco. Al cazador. Al guerrero. Y vuelta a empezar, treinta o cuarenta
        veces, sin prisa pero sin parar.
        CAMBIAR DE BOT ES LO QUE LO DISPARABA: lanzar diez veces seguidas por el
        MISMO bot no pasaba por el camino roto. Si repites con uno solo y no
        pasa nada, no has probado esto.
        notas:

[ ] A2. Lo mismo desde el estado B (varios seleccionados), pulsando huecos de
        columnas distintas. Es el mismo camino con otra boca.
        notas:

[ ] A3. Sigue jugando media hora larga con la consola. Si el servidor aguanta
        una sesion normal despues de A1 y A2, esto esta cerrado.
        SI SE VUELVE A CAER: no hace falta describir el sintoma -- lo que vale
        es el fichero nuevo de `C:\Server\dist\Crashes\`. Con eso se lee la
        direccion del fallo y se compara con la de anoche (RVA 0x14A462); si es
        otra, es otro fallo distinto y hay por donde empezar.
        notas:


B. EL CASTEO QUE SE CORTABA (era C6 de la 23)
---------------------------------------------

[ ] B1. Selecciona a la sanadora, arma una curacion (la luz circular), y
        pincha a alguien.
        **El hechizo tiene que TERMINAR**: barra de lanzamiento entera y la
        vida sube. Antes empezaba la animacion y se cancelaba a medio segundo.
        notas:

[ ] B2. Igual con un hechizo largo de dano (un rayo, un fuego) sobre un enemigo.
        notas:

[ ] B3. **Y con el bot siguiendote mientras andas.** Este es el caso que fallaba
        de verdad: el bot se recoloca para seguirte, ese movimiento cortaba el
        casteo. Anda un poco mientras lanza.
        Si sigue cortandose SOLO en este caso, el plazo de silencio de la IA se
        queda corto o algo mas la esta despertando.
        notas:

[ ] B4. Y LO QUE NO DEBE PASAR: que el bot se quede tonto DESPUES de lanzar. Su
        IA se calla el tiempo del casteo mas un pelin; pasado eso tiene que
        volver a pelear y a seguirte solo.
        Si se queda plantado varios segundos de mas, el plazo se pasa.
        notas:

[ ] B5. Un hechizo INSTANTANEO (un grito, un golpe) no debe frenar al bot nada:
        a esos no se les calla la IA a proposito.
        notas:


C. LOS HECHIZOS: DE DONDE SALEN AHORA
-------------------------------------

[ ] C1. **Neferite (tu heroe) YA TIENE HECHIZOS.** Seleccionalo -- o no
        selecciones nada, que es lo mismo -- y la fila tiene que
        llenarse.
        Era el fallo: tu propio personaje tenia otro camino de lectura y ese
        camino se quedaba vacio en silencio.
        notas:

[ ] C2. Y los suyos LLEVAN TIPO. Click derecho sobre un hueco suyo: la lista
        tiene que decir "sobre un enemigo", "sin objetivo"... igual que la de
        los bots. Antes, para tu personaje, todo salia "sin objetivo".
        notas:

[ ] C3. **La lista es mucho mas larga que antes.** Click derecho sobre un hueco:
        salen primero los de su barra de acciones, en el orden de la barra, y
        detras TODO LO QUE EL PERSONAJE SEPA, por nombre.
        Es la respuesta a tu pregunta: playerbots no tiene ninguna lista de
        hechizos que consultar -- elige accion por accion -- asi que lo que se
        manda es su libro, que si crece solo al subir de nivel.
        notas:

[ ] C4. **LA LISTA VA POR PAGINAS.** Abajo del todo hay una fila "mas ..." con
        cuantas quedan, y arriba "... anteriores" cuando no estas en la primera.
        Pulsarlas cambia de pagina SIN cerrar el desplegable.
        Si la lista sale cortada por abajo o se sale de la pantalla, la
        paginacion no esta entrando.
        notas:

[ ] C5. En la lista NO deben salir: pasivos, oficios (Herreria, Cocina),
        idiomas, ni hechizos de mascota en el cazador o el brujo.
        Tampoco rangos viejos: de cada hechizo, solo el que el personaje usa.
        Si sale un rango viejo, el filtro del nucleo no esta haciendo lo que
        dice su propio comentario y hay que mirarlo.
        notas:

[ ] C6. Elegir uno de los NUEVOS (uno que no estuviera en su barra) y lanzarlo.
        Tiene que salir igual que los de la barra.
        notas:

[ ] C7. La configuracion sigue persistiendo: pon hechizos, `/reload`, siguen ahi.
        notas:


D. LA SALA CONTRA EL BOCETO RETOCADO
-----------------------------------

Todo esto sale de MEDIR la imagen retocada pixel a pixel, no de elegirlo, y el
reparto entero esta comprobado fuera del juego con `sim/hall_layout.py`. Lo que
queda por ver es si gusta, no si cuadra.

Numeros de referencia (con la barra que sale sola, `grow` 3): la columna mide
272 de dibujo, la separacion 78, el icono 84 y la fila sale de **catorce**
huecos que llenan el ancho exacto.

[ ] D1. **Los iconos de hechizo son mas pequenos.** De 63 px a 47.
        63 era exactamente un boton de accion del cliente -- que sonaba bien y
        al lado de todo lo demas de la consola era la pieza mas grande.
        notas:

[ ] D2. **La columna de los cinco es ESTRECHA.** De 276 px a 168.
        El texto NO se ha tocado: sigue con el tamano que se subio anoche. Lo
        que cambia es que ahora lo llena en vez de flotar en media fila vacia.
        Si algun nombre se corta o se monta con el numero de la derecha, decir
        CUAL -- se le reservan 88 de dibujo al numero y puede quedarse corto.
        notas:

[ ] D3. **Separacion de sobra entre la columna y el centro**: de 27 px a 44.
        notas:

[ ] D4. **LA FILA DE HUECOS LLENA EL ANCHO.** Con `grow` 3 tienen que salir
        CATORCE, no diez, y el ultimo tiene que acabar justo en el borde
        derecho de la zona.
        Es el cambio de fondo del boceto: antes eran diez fijos centrados y
        sobraba media consola. `/rts hall` dice cuantos hay.
        **Con `grow` 4 salen diecinueve.** Si son demasiados,
        `/rts hall slots 12` los topa -- y lo guardado no se pierde, vuelve al
        subir el tope.
        notas:

[ ] D5. **TODO PEGADO A LA IZQUIERDA Y ALINEADO.** El borde izquierdo del marco
        del personaje, el de la raya, el del primer hueco y el del primer macro
        son EL MISMO.
        Es lo contrario de lo que se hizo anoche (centrar), y por eso hay que
        mirarlo: si algo se ve corrido respecto de los otros tres, decir cual.
        notas:

[ ] D6. **La raya horizontal mide lo que el bloque**, no toda la zona: empieza y
        acaba donde la fila de huecos.
        notas:

[ ] D7. **Los cuatro macros llevan borde fino y se separan mas.** Sin borde, al
        quitarles el icono, cuatro rectangulos oscuros sobre panel oscuro se
        leen como huecos y no como botones.
        notas:

[ ] D8. **Los marcos de arriba son bastante mas grandes** (de 47 px de alto a
        120, que es lo que mide el marco de jugador del propio cliente).
        notas:

[ ] D9. **LA RAYA VERTICAL ENTRE SELECCIONADOS.** Selecciona dos: una raya
        vertical en medio. Con cinco, cuatro rayas.
        Tienen que quedar CENTRADAS en el hueco entre columnas y sin tocar
        ninguna: una raya pegada a una columna se lee como su borde y se nota
        antes la asimetria que la raya.
        Y se quedan cortas por arriba y por abajo a proposito (18 de dibujo).
        notas:

[ ] D9b. **AIRE A LA DERECHA.** El ultimo hueco y el ultimo macro tienen que
         dejar el mismo margen contra el arte que hay entre la columna y los
         hechizos (44 px). Antes llegaban al borde y se veian recortados.
         Con este margen la fila pasa a TRECE huecos con `grow` 3 -- que es
         exactamente lo que tenia el boceto retocado.
         notas:

[ ] D9c. **Los marcos de arriba bajan 14** y ya no van pegados al filo del arte.
         notas:

[ ] D9d. **La columna del estado B respira**: el nombre no toca el borde de
         arriba, el 2x2 no toca los macros y los dos macros no se tocan.
         El icono baja de 47 a 44 px para pagarlo -- el alto de la sala es fijo.
         notas:

[ ] D10. Con `/rts bar grow 2` salen ocho huecos y sigue cuadrando. Con `grow 1`
         salen tres y **los macros desaparecen** -- correcto: a ese ancho salen
         a 31 px y no cabe "Sigueme". `/rts hall` lo dice.
         El estado B necesita `grow` 2 para cinco columnas.
         Volver con `/rts bar grow 3` o `4`.
         notas:


E. LOS MACROS, SIN ICONO
------------------------

[ ] E1. Las cuatro barras anchas de abajo llevan **solo el nombre, centrado**.
        Sin el iconito de la izquierda.
        notas:

[ ] E2. **El desplegable de elegir accion SI sigue llevando iconos** (click
        derecho sobre un macro). Es deliberado: once opciones en una lista se
        encuentran antes con dibujo.
        notas:

[ ] E3. Los nombres no se parten en dos lineas ni se salen de la barra.
        notas:

[ ] E4. Y siguen funcionando: Cuidar, Quieto, Sigueme, Ataca sobre el
        seleccionado.
        notas:


F. EL CLICK DERECHO EN LA LISTA
-------------------------------

[ ] F1. **Click derecho sobre una fila del grupo ya no hace nada.** Era lo que
        hacia primario sin seleccionar, y lo pediste fuera.
        notas:

[ ] F2. **El primario sigue funcionando**, solo que ahora lo pone la seleccion:
        seleccionar a UNO le hace primario (su nombre en amarillo y sus
        hechizos abajo); con varios o con nada, no cambia.
        notas:

[ ] F3. Lo que el derecho SI sigue haciendo: cancelar un hechizo armado y
        cancelar el "Cuidar" pendiente. Eso va antes y no se toco.
        notas:


G. LA COLA -- SIN PROBAR DESDE LA RONDA 23
------------------------------------------

Quedo `[-]` entera. Se repite porque el crash pudo estar tapandola: si el
servidor se caia al rato, cualquier cosa de aqui podia parecer un fallo suyo.

[ ] G1. **El caso que la justifica.** Manda al grupo a beber (rejilla 4x4,
        "Beber"), espera a que se sienten, y pulsa un hechizo de uno.
        Tiene que salir en cuanto se levante, no fallar. Sin cola esto fallaba
        SIEMPRE la primera vez.
        notas:

[ ] G2. Pulsar un hechizo mientras el bot esta lanzando otro: sale en cuanto
        acabe.
        notas:

[ ] G3. **Un hechizo que no puede salir DICE por que.** Curacion sobre alguien
        lejisimos: a los 8 s tiene que salir "no pudo lanzar X: fuera de
        alcance".
        Si no sale NADA, la cola se esta comiendo el fallo.
        notas:

[ ] G4. **Y NO DEBE ANCLAR AL BOT.** Con esa curacion en cola, el sanador tiene
        que poder seguir moviendose. Si se queda clavado los 8 segundos, la
        guarda de alcance no esta corriendo.
        OJO, ESTO Y B4 SE PARECEN Y NO SON LO MISMO: aqui el bot no debe pararse
        MIENTRAS ESPERA; alli no debe quedarse tonto DESPUES de lanzar. Si los
        dos fallan a la vez, decirlo -- serian el mismo plazo mal puesto.
        notas:

[ ] G5. Un hechizo de mascota no aparece en la lista de un cazador o un brujo.
        notas:


I. EL CAMBIO DE PERSONAJE: NOMBRE Y CLASE
-----------------------------------------

El cliente guarda tu nombre, tu clase y tu raza en una ficha aparte que solo
rellena la pantalla de seleccion de personaje -- justo el paso que el cambio se
salta. Asi que `UnitName("player")` y `UnitClass("player")` se quedan con los del
personaje con el que ARRANCASTE la sesion. Todo lo demas (vida, retrato,
hechizos) sale del objeto y es correcto, y esa mezcla es lo que hacia el sintoma
raro: retrato de uno, nombre del otro.

[ ] I1. Salta a un personaje de OTRA CLASE (Neferite guerrero -> Bob mago).
        Su fila en la columna tiene que salir con el color de SU clase, no con
        el de la clase con la que entraste.
        notas:

[ ] I2. Y el marco de arriba tiene que decir SU nombre, no el viejo.
        notas:

[ ] I3. **`/rts whoami` imprime ahora las dos respuestas**: lo que dice el
        cliente y lo que decimos nosotros. Despues de un cambio tienen que
        DIFERIR -- eso significa que la correccion esta actuando. Si coinciden
        las dos en el nombre viejo, no esta corrigiendo nada.
        notas:

[ ] I4. **`/reload` despues del cambio y vuelve a mirar.** El color aprendido se
        guarda en disco justo por esto: en memoria se perderia, y tu propio
        personaje no aparece en tu grupo, asi que no habria de donde
        reaprenderlo. Si el color se estropea SOLO tras un `/reload`, lo que
        falla es el guardado.
        notas:

[ ] I5. Salta a un tercero encadenando (Bob -> Kirinah). Sin recargar entre
        medias.
        notas:


H. QUE NO SE HAYA ROTO NADA
---------------------------

[ ] H1. Minimapa, railes y rejilla 4x4 donde estaban.
        notas:

[ ] H2. La caja de arrastre para seleccionar en el mundo sigue funcionando.
        notas:

[ ] H3. Las rutas con shift + click derecho siguen funcionando.
        notas:

[ ] H4. El cambio de personaje (`/rts swap`) sigue funcionando y al volver la
        sala se rehace con el heroe nuevo el primero.
        notas:

[ ] H5. Salir del modo RTS devuelve la interfaz de Blizzard.
        notas:

[ ] H6. Lo que ya funcionaba de la sala: seleccion con click y doble click, la
        luz circular, el segundo click en la lista y en el mundo, Alt para
        lanzar sobre si mismo, el aviso de tipo equivocado, el foco (Cuidar) y
        la rejilla 4x4 al grupo entero.
        notas:
